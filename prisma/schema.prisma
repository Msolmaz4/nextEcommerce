datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

generator client {
  provider        = "prisma-client-js"

}
//burda zapraken doc bak burasi iicndoc sayasinin yndan MONGODBsupport var on agore yap
model Account {
  id                 String  @id @default(auto()) @map("_id") @db.ObjectId
  userId             String   @db.ObjectId
  type               String
  provider           String
  providerAccountId  String
  refresh_token      String?   @db.String
  access_token       String?  @db.String
  expires_at         Int?
  token_type         String?
  scope              String?
  id_token           String?  @db.String
  session_state      String?
//burda  references: [user_id] duye.teme yaptim 
  user User @relation(fields: [userId], references: [user_id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}


model User {
  user_id        String   @id @default(auto()) @map("_id") @db.ObjectId
  name          String?
  email         String?   @unique
  emailVerified DateTime?
  image         String?
  hashedPassword String?
  createdAt      DateTime @default(now())
  updateAt       DateTime @default(now())
  role           Role @default(USER)     
  accounts      Account[]
 
}

enum Role{
    USER
    ADMIN
}
//npx prisma db push  ayarlamalardan sonra bunu yap 
//bunu yaptikatan sonra doc kullandim ahayta aldin arastirdim https://next-auth.js.org/v3/adapters/prisma bu adreskdi doc kulln bundan sonra
